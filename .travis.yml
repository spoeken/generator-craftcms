language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-craftcms' ]; then cd .. && eval "mv $currentfolder generator-craftcms" && cd generator-craftcms; fi

